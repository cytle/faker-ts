{
  "name": "json-schema-mock.ts",
  "scripts": {
    "prepublishOnly": "npm run build",
    "test": "npm run build && mocha -t 5000 --require source-map-support/register dist/test",
    "debug": "node --inspect-brk -r ts-node/register lib/koaServer.ts",
    "run": "ts-node json-schema-mock.ts",
    "serve": "cd tests && ts-node ../lib/koaServer.ts",
    "build": "tsc -p .",
    "lint": "tslint --project tsconfig.json -c tslint.json --exclude '**/*.d.ts'"
  },
  "dependencies": {
    "faker": "^4.1.0",
    "json-schema-faker": "^0.5.0-rc17",
    "koa-router": "^7.4.0",
    "typescript": "^3.5.2",
    "typescript-json-schema": "^0.38.3"
  },
  "devDependencies": {
    "@types/faker": "^4.1.5",
    "@types/koa": "^2.0.48",
    "@types/node": "^12.0.10",
    "koa": "^2.7.0",
    "ts-node": "^8.3.0",
    "tslint": "^5.18.0"
  }
}
